package g2p

// Engine represents the core G2P transliteration engine
type Engine struct {
	// TODO: Add engine state (trie, dict, rules)
}

func NewEngine(opts Options) *Engine {
	return &Engine{}
}
